export type FlashVariant = "default" | "success" | "warning" | "danger";

export type FlashStyles = {
  color: string;
  backgroundColor: string;
  borderColor: string;
  svg: {
    color: string;
  };
};
